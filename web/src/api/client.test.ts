import { AxiosError, AxiosHeaders } from "axios";
import { describe, expect, it, vi } from "vitest";
import { api, fetchPhotoFaceThumbnail, PhotoFaceThumbnailPendingError } from "./client";
describe("fetchPhotoFaceThumbnail pending classification",()=>{it("maps only marked 404 to typed pending",async()=>{const spy=vi.spyOn(api,"get").mockRejectedValueOnce(new AxiosError("pending","ERR_BAD_REQUEST",undefined,undefined,{status:404,statusText:"Not Found",headers:new AxiosHeaders({"x-knox-thumbnail-pending":"1","retry-after":"5"}),config:{headers:new AxiosHeaders()},data:new Blob()}));await expect(fetchPhotoFaceThumbnail(7)).rejects.toBeInstanceOf(PhotoFaceThumbnailPendingError);spy.mockRestore();});it("keeps unmarked forbidden and not-found permanent",async()=>{for(const status of [403,404,500]){const spy=vi.spyOn(api,"get").mockRejectedValueOnce(new AxiosError("permanent","ERR_BAD_REQUEST",undefined,undefined,{status,statusText:"error",headers:new AxiosHeaders(),config:{headers:new AxiosHeaders()},data:new Blob()}));await expect(fetchPhotoFaceThumbnail(7)).rejects.not.toBeInstanceOf(PhotoFaceThumbnailPendingError);spy.mockRestore();}});});
