# 智能全媒体内容管理系统（KNOX）

## 分布式转码集群部署手册（已补全：文件传输协议章节）

---

# 1 文档概述

## 1.1 文档说明

本文档为**KNOX分布式媒体处理与转码集群**的标准部署、运维、扩容手册，适用于生产环境、测试环境、私有化部署。
基于：SpringBoot、Redis Queue、FFmpeg、MySQL、OpenSearch
支持：**媒体服务与转码服务物理分离、多节点分布式、水平扩容、高可用**

## 1.2 适用范围

- 系统管理员
- 运维工程师
- 开发工程师
- 项目交付实施

## 1.3 架构总览

- **knox-media-service**：媒体核心（库管理、上传、扫描、元数据、搜刮、播放）
- **knox-transcode-service**：分布式转码节点（可多实例部署）
- **knox-task-center**：任务调度中心（分发、负载均衡、状态管理）
- **中间件**：MySQL、Redis、OpenSearch、OSS/NAS

---

# 2 分布式架构说明

## 2.1 核心组件职责

1. **媒体服务（media）**
  - 上传、扫描、元数据解析、信息搜刮
    - 媒体库、权限、密级、预览、播放
2. **转码服务（transcode）**
  - 仅负责：FFmpeg 执行、异步转码、实时转码、HLS切片
    - 无状态、可无限多开
3. **任务中心（task-center）**
  - 任务入队、负载均衡、进度回调、状态持久化
4. **存储层**
  - MySQL：业务数据、任务状态
    - Redis：任务队列、分布式锁、进度缓存
    - OSS/NAS：媒体文件、转码后文件

## 2.2 分布式转码流程

1. 媒体服务提交转码任务
2. 任务中心写入 Redis 队列
3. 转码节点监听队列并抢占任务
4. 转码中实时更新进度
5. 转码完成回调媒体服务更新地址
6. 前端可获取多码率播放地址

---

# 3 媒体服务与转码服务文件传输协议（补全完整版）

## 3.1 核心设计原则

**转码服务与媒体服务之间不直接传输二进制文件**，采用**共享存储/对象存储**架构，仅传递**文件唯一标识、存储路径、元数据**，不传输文件流。
该方案为 Jellyfin / Emby / Plex 分布式转码标准方案，具备高性能、零拷贝、高稳定特性。

## 3.2 支持的文件传输与访问协议

### 3.2.1 NFS 协议（企业内网首选）

- 协议类型：**NFSv3 / NFSv4**
- 适用场景：内网、同机房、低延迟、高速存储
- 实现方式：媒体服务与转码服务挂载同一共享目录
- 优点：性能最高、文件零拷贝、延迟最低
- 用途：源文件读取、转码输出文件写入

### 3.2.2 SMB/CIFS 协议（Windows 共享）

- 协议类型：**SMB2 / SMB3**
- 适用场景：混合系统环境、Windows 存储服务器
- 用途：媒体文件共享访问

### 3.2.3 S3/OSS 对象存储协议（云端/跨网首选）

- 协议类型：**S3 RESTful API / HTTP**
- 适用场景：公有云、混合云、跨机房、弹性扩容
- 实现方式：媒体服务上传至 OSS，转码服务通过 S3 SDK 读写
- 优点：无限容量、高可用、易扩展
- 用途：源文件拉取、转码结果回传

### 3.2.4 HTTP/HTTPS 协议（通用拉取协议）

- 协议类型：**HTTP/1.1、HTTP/2**
- 适用场景：无共享存储、跨网络、第三方文件源
- 实现方式：媒体服务提供带签名的文件 URL，转码服务通过 Range 分片下载
- 用途：只读拉取源文件

### 3.2.5 任务与元数据传输协议（不传输文件）

- **Redis 协议**：任务分发、队列、锁
- **HTTP/HTTPS 协议**：状态回调、进度上报
- **WebSocket 协议**：实时进度推送
- **JSON**：元数据交互格式

## 3.3 标准数据交互流程

1. 媒体服务将文件存入 **NFS/SMB/OSS**
2. 媒体服务通过 **Redis** 发送任务：`fileId + storagePath + metadata`
3. 转码服务通过 **NFS/SMB/OSS/HTTP** 读取源文件
4. 转码完成后，输出文件写入 **NFS/SMB/OSS**
5. 转码服务通过 **HTTP** 回调媒体服务更新路径与状态
6. 媒体服务更新数据库，对外提供播放地址

## 3.4 协议选型推荐

- **内网生产环境**：NFS + Redis + HTTP
- **云端生产环境**：OSS/S3 + Redis + HTTP
- **测试/开发环境**：本地目录共享 + HTTP

---

# 4 环境要求

## 4.1 硬件配置（最低）

- CPU：4核（转码节点建议 16核+）
- 内存：8G（转码节点建议 32G+）
- 存储：SSD 500G+
- 网络：千兆网卡

## 4.2 软件依赖

- JDK 17+
- MySQL 8.0+
- Redis 6.2+
- FFmpeg 6.0+
- OpenSearch 2.11+

---

# 5 部署结构与端口规划


| 服务                     | 端口             | 说明            | 可集群    |
| ---------------------- | -------------- | ------------- | ------ |
| knox-studio            | 8080           | 内容管理后台        | 否（单实例） |
| knox-engine            | 8081           | 前台渲染/Headless | 是      |
| knox-deployer          | 8082           | 发布/索引         | 是      |
| knox-media-service     | 8900           | 媒体核心服务        | 是      |
| knox-task-center       | 9100           | 任务调度中心        | 否（单实例） |
| knox-transcode-service | 9000/9001/9002 | 转码节点          | 无限扩容   |


---

# 6 前置依赖安装

## 6.1 FFmpeg 安装（所有转码节点必须安装）

```
# CentOS
yum install -y ffmpeg

# Ubuntu
apt install -y ffmpeg

# 验证
ffmpeg -version
```

## 6.2 中间件启动

```
docker-compose -f docker-compose.yml up -d
```

包含：MySQL、Redis、OpenSearch

---

# 7 数据库初始化

执行脚本：`knox_media_transcode.sql`

```sql
-- 媒体库
CREATE TABLE media_library ...
-- 媒体条目
CREATE TABLE media_item ...
-- 转码任务
CREATE TABLE media_transcode_task ...
-- 转码文件
CREATE TABLE media_transcode_file ...
```

---

# 8 配置文件说明

## 8.1 公共配置（application-common.yml）

```yaml
spring:
  redis:
    host: 127.0.0.1
    port: 6379
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/knox
```

## 8.2 转码服务配置

```yaml
transcode:
  async:
    enabled: true
  thread-pool:
    core-size: 2
    max-size: 4
  output-dir: ./transcode
  hls-enabled: true
```

---

# 9 服务启动顺序（必须严格遵守）

1. MySQL、Redis、OpenSearch
2. knox-task-center（9100）
3. knox-media-service（8900）
4. knox-transcode-service（9000、9001…）
5. knox-studio、knox-engine、knox-deployer

## 9.1 启动命令示例

```
java -jar knox-task-center.jar
java -jar knox-media-service.jar
java -jar knox-transcode-service.jar --server.port=9000
java -jar knox-transcode-service.jar --server.port=9001
```

---

# 10 分布式扩容指南

## 10.1 转码集群扩容

1. 复制 transcode .jar
2. 修改端口 9000 → 9001 → 9002…
3. 启动即可
4. 任务中心自动负载均衡到所有节点

## 10.2 媒体服务扩容

启动多个 media-service 实例，使用 Nginx 负载均衡即可。

---

# 11 功能验证

## 11.1 上传媒体

```
POST http://ip:8900/api/media/upload
```

## 11.2 创建异步转码任务

```
POST http://ip:8900/api/media/transcode/async
```

## 11.3 查看任务列表

```
GET http://ip:8900/api/media/task/list
```

## 11.4 播放 HLS 转码流

```
http://ip:8900/api/media/play/hls/{fileId}.m3u8
```

---

# 12 运维监控

## 12.1 转码节点状态

- 查看日志：transcode-service.log
- 查看队列长度：Redis key: `transcode:queue`
- 查看任务进度：`transcode:progress:{taskId}`

## 12.2 常见问题

- 转码失败：检查 FFmpeg 是否安装
- 任务不执行：检查 Redis 连接
- 无法播放：检查文件路径、权限、HLS 切片

---

# 13 高可用方案

- 转码节点无状态，支持宕机自动故障转移
- 任务中心单点，可通过 Keepalived 实现高可用
- 媒体服务集群部署，Nginx 负载均衡
- 存储使用 NAS/OSS 共享存储

---

# 14 交付清单

- knox-media-service.jar
- knox-transcode-service.jar
- knox-task-center.jar
- 部署手册
- 数据库脚本
- Docker 部署脚本

---

# 15 附录：术语说明

- 异步转码：后台批量转码
- 实时转码：根据客户端适配动态转码
- HLS：HTTP 流媒体协议
- 分布式转码：多机器并行转码
- 媒体库：统一管理音视频、图片、文档
- NFS：网络文件系统协议
- OSS/S3：对象存储协议

