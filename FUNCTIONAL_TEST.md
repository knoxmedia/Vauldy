# Knox Media 功能测试计划

> 测试日期: ________ | 测试人: ________ | 后端: http://127.0.0.1:8200 | 前端: http://localhost:5173

本文档按当前已实现能力整理，便于 **逐项手工回归**。测试时在每条记录下 **通过 / 失败 / 阻塞原因** 即可。

---

## 1. 环境与前置条件

- [ ] **1.1** 确认后端 `knox-media` 已启动（默认 `http://127.0.0.1:8200`），`GET /health` 返回 `status: ok`
  - 结果: ___
- [ ] **1.2** 确认 `config.yml` 中 `ffmpeg`、`ffprobe` 路径有效（转码、截图、预览依赖）
  - 结果: ___
- [ ] **1.3** **开发模式**：前端 `pnpm dev` / `npm run dev`（Vite，`http://localhost:5173`），且 `vite.config.ts` 已将 `/api`、`/health`、`/uploads`、`/metadata/library` 代理到后端
  - 结果: ___
- [ ] **1.4** **集成模式**：使用后端嵌入的 `web/dist` 同端口访问
  - 结果: ___
- [ ] **1.5** 数据库已初始化，至少存在可登录账号（如种子用户 `admin` / `viewer`）
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 2. 认证与会话

- [ ] **2.1** 打开 `/login`，使用错误密码登录 → 提示失败，不进入首页
  - 结果: ___
- [ ] **2.2** 使用正确管理员账号登录 → 进入首页，侧栏含「媒体库」「上传」等管理入口
  - 结果: ___
- [ ] **2.3** 使用普通用户账号登录（若已创建）→ 进入首页，**无**管理员专属菜单
  - 结果: ___
- [ ] **2.4** 登录后刷新页面 → 仍保持登录（Token 有效）
  - 结果: ___
- [ ] **2.5** 用户菜单 → **退出** → 回到登录页，再访问受保护路由会跳转登录
  - 结果: ___
- [ ] **2.6** 未登录直接访问 `/browse` → 跳转 `/login`
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 3. 首页

- [ ] **3.1** 查看 **媒体库** 横向卡片 → 显示库名、类型、条目数；若有扫描任务显示进度
  - 结果: ___
- [ ] **3.2** 点击某一媒体库卡片 → 跳转 `/browse?library_id=<id>`
  - 结果: ___
- [ ] **3.3** **继续观看**（有播放进度时）→ 卡片显示 16:9 封面图、进度条、标题、百分比
  - 结果: ___
- [ ] **3.4** 点击继续观看卡片 → 进入 `/detail/<id>`；点击播放按钮进入 `/player/<id>?t=...`
  - 结果: ___
- [ ] **3.5** **最近添加** → 竖版封面图正常；点击进详情或播放
  - 结果: ___
- [ ] **3.6** 点击「最近添加 → 更多」→ 进入 `/browse?sort=recent`
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 4. 浏览媒体（`/browse`）

- [ ] **4.1** 不按库筛选打开浏览 → 列出当前账号有权访问的媒体（含 `poster_url` 或回退本地海报）
  - 结果: ___
- [ ] **4.2** 切换排序：标题 / 添加日期 / 播放日期等 → 列表顺序变化合理
  - 结果: ___
- [ ] **4.3** 切换视图：**海报** / **缩略图** / **列表** / **表格** → 均能展示；海报与缩略图模式有封面图
  - 结果: ___
- [ ] **4.4** URL 带 `library_id` → 仅显示该库媒体
  - 结果: ___
- [ ] **4.5** 搜索框（若有 `q` 参数或页面内筛选）→ 标题过滤生效
  - 结果: ___
- [ ] **4.6** **播放** / **详情** → 分别进入播放器与详情页
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 5. 媒体详情（`/detail/:id`）

- [ ] **5.1** 打开任意视频详情 → 展示标题、分辨率、编码、时长、简介（若有刮削）
  - 结果: ___
- [ ] **5.2** **海报** → 优先 `scrape.extra.poster`，否则 `scrape.poster`，再否则 `/uploads/posters/{id}.jpg`
  - 结果: ___
- [ ] **5.3** **背景图 / Logo**（若有元数据）→ 展示；加载失败时有降级
  - 结果: ___
- [ ] **5.4** **继续播放** / **从头播放**（有进度时）→ 跳转播放器并带合理起点
  - 结果: ___
- [ ] **5.5** **收藏**（若页面提供）→ 状态与 `/favorites` 一致
  - 结果: ___
- [ ] **5.6** 相关推荐（同库近期）→ 列表可点击
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 6. 播放（`/player/:id`）

- [ ] **6.1** 播放 **浏览器可直接解码** 的 MP4（H.264+AAC）→ 可走直链，正常起播
  - 结果: ___
- [ ] **6.2** 播放 **WMV / 非常规格式** → 接口返回需 HLS 自适应；任务 `waiting` → `running` → `done` 后可播
  - 结果: ___
- [ ] **6.3** Chromium 系浏览器播 HLS → 使用 xgplayer-hls（MSE），非裸 `<video src=.m3u8>`
  - 结果: ___
- [ ] **6.4** 打开开发者工具，确认 master / 子 playlist / ts 请求带 `access_token`（或 Cookie 同源策略下合法）→ 无大面积 401/403
  - 结果: ___
- [ ] **6.5** **预览进度条缩略图**（库开启 `preview_extract` 且 `preview_task` 完成）→ `/preview` 返回 sprite/vtt 相关信息时可显示
  - 结果: ___
- [ ] **6.6** **DRM 内容**（若库启用 DRM 且已打包完成）→ Shaka 等 DRM 路径可试播（需对应 CDM/证书环境）
  - 结果: ___
- [ ] **6.7** 播放中触发失败后的 **源文件回退**（若业务已实现）→ 行为符合产品设计
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 7. 收藏与播放列表

- [ ] **7.1** 在详情或列表将媒体 **加入收藏** → `/favorites` 中出现
  - 结果: ___
- [ ] **7.2** 取消收藏 → 从列表移除
  - 结果: ___
- [ ] **7.3** **播放列表**（`/playlists`）→ 创建/编辑/删除列表与条目（以当前 UI 为准）
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 8. 搜索（`/search`）

- [ ] **8.1** 输入关键词搜索 → 返回匹配媒体或可播放项
  - 结果: ___
- [ ] **8.2** 点击结果进入详情或播放 → 路由正确
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 9. 设置（`/settings`）

- [ ] **9.1** 查看账号相关设置 → 与角色相符的说明或选项
  - 结果: ___
- [ ] **9.2** 修改密码等（若提供）→ 保存后可用新密码登录
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 10. 媒体库管理（管理员，`/library`）

- [ ] **10.1** **新建媒体库**：名称、类型、路径、是否自动扫描等 → 保存成功，首页可见
  - 结果: ___
- [ ] **10.2** 编辑库：**预览截取**（`preview_extract`）→ 开启后新入库媒体可排队 `preview_task`
  - 结果: ___
- [ ] **10.3** **JIT 入库准备** / **DRM** 等开关（若使用）→ 与扫描、上传后触发的任务一致
  - 结果: ___
- [ ] **10.4** **扫描库** → 生成 `scan_task`，媒体条目增加；扫描完成后继续观看/列表数据更新
  - 结果: ___
- [ ] **10.5** 删除库（谨慎）→ 符合产品确认流程
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 11. 上传（管理员，`/upload`）

- [ ] **11.1** **单文件上传** 至指定库 → 返回媒体 ID，`media` 表有记录
  - 结果: ___
- [ ] **11.2** **分片上传 + 合并** → 大文件合并成功，路径正确
  - 结果: ___
- [ ] **11.3** 上传完成后 **自动队列**：刮削、`preview_task`（库开启时）、本地海报截帧、字幕任务（配置开启时）→ 任务管理或 DB 中可见
  - 结果: ___
- [ ] **11.4** 新建上传目录（`mkdir`）→ 目录存在于库根下
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 12. 媒体资料管理（管理员，`/media-manager`）

- [ ] **12.1** 检索并打开某一媒体 → 可编辑标题、结构化字段、海报/背景/Logo URL
  - 结果: ___
- [ ] **12.2** 保存后返回详情页 → `meta_json` 与展示一致
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 13. 任务管理（管理员，`/tasks`）

- [ ] **13.1** 查看 **转码任务** → 状态、`error_message`、进度合理
  - 结果: ___
- [ ] **13.2** 失败任务 **重试 / 取消**（若 UI 提供）→ 后端状态更新
  - 结果: ___
- [ ] **13.3** **预览任务** → `waiting` → `ready` / `failed`；重试入口可用
  - 结果: ___
- [ ] **13.4** **刮削任务** → 完成后详情页元数据更新
  - 结果: ___
- [ ] **13.5** **字幕任务** → 与配置一致
  - 结果: ___
- [ ] **13.6** **扫描任务** → 与库扫描联动
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 14. 控制台与其它管理页（管理员）

- [ ] **14.1** **控制台** `/console` → 概览或聚合数据加载正常
  - 结果: ___
- [ ] **14.2** **访问日志** `/access-logs` → 分页/筛选可用
  - 结果: ___
- [ ] **14.3** **DRM 许可证审计** `/drm-license-audit` → 有 DRM 时可追溯
  - 结果: ___
- [ ] **14.4** **API 凭证** `/api-credentials` → 创建/吊销客户端，OAuth 播放链路可测
  - 结果: ___
- [ ] **14.5** **用户管理** `/users` → 增删改用户、重置密码
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 15. 权限与多库隔离

- [ ] **15.1** 为普通用户配置 **仅部分媒体库** → 浏览/详情/播放无法访问未授权库
  - 结果: ___
- [ ] **15.2** **家长控制 / PIN**（若启用）→ 受限内容需解锁后方可播放
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 16. 静态资源与海报

- [ ] **16.1** 浏览器直接访问 `http://127.0.0.1:8200/uploads/posters/<媒体ID>.jpg` → 若已截帧则 200
  - 结果: ___
- [ ] **16.2** 开发模式下通过 `http://localhost:5173/uploads/posters/<id>.jpg` → 代理到后端，非 404
- [ ] **16.3** 刮削落盘海报：`http://localhost:5173/metadata/library/<shard>/<id>/poster.jpg` → 代理到后端，非 404
  - 结果: ___

> 模块结果: 通过 __ / 失败 __ / 阻塞 __

---

## 17. 回归清单（发布前快速扫一遍）

- [ ] 登录 / 退出
- [ ] 首页：媒体库、继续观看、最近添加 **带图**
- [ ] 浏览：多视图 + 封面
- [ ] 详情：海报与背景
- [ ] 播放：常见 MP4 + 至少一种需 **HLS 转码** 的格式
- [ ] 上传 → 列表/详情可见 → 海报或 `/uploads/posters` 其一可用
- [ ] （管理员）扫描、任务、用户

---

## 附录：常用账号（示例）

以当前环境种子数据为准；默认演示账号见项目 README 或首次部署说明（如 `admin` / `admin123`）。**生产环境务必修改默认密码。**

---

## API 后端验证结果

> 以下为自动化 API 验证结果（通过 curl 对 http://127.0.0.1:8200 执行，日期: 2026-05-05）

### 测试环境概况

| 项目 | 值 |
|------|-----|
| 后端版本 | knox-media (Gin) |
| 数据库 | SQLite (data/knox-media.db) |
| 媒体库数 | 1 (movies, 190 items) |
| 注册用户 | 2 (admin, viewer) |
| ffmpeg/ffprobe | tools/ffmpeg/bin/ (可用) |
| shaka-packager | tools/shaka-packager/ (可用) |
| web/dist | 存在 (集成模式可用) |

### API 端点验证

| 端点 | 方法 | HTTP | 状态 | 说明 |
|------|------|------|------|------|
| `/health` | GET | 200 | 通过 | `{"status":"ok","service":"knox-media"}` |
| `/api/v1/user/login` (错误) | POST | 200 | 通过 | 返回 `{"error":"invalid credentials"}` |
| `/api/v1/user/login` (正确) | POST | 200 | 通过 | 返回 token, role=admin, 168h 有效期 |
| `/api/v1/library` (无认证) | GET | 200 | 通过 | 返回 `{"error":"unauthorized"}` (符合预期) |
| `/api/v1/library` (认证) | GET | 200 | 通过 | 返回 1 个库 (movies, 190 items) |
| `/api/v1/user/info` | GET | 200 | 通过 | 返回 admin 角色信息 |
| `/api/v1/media?limit=3` | GET | 200 | 通过 | 返回 3 条媒体记录含元数据 |
| `/api/v1/media/191` | GET | 200 | 通过 | 返回完整 meta_json (ffprobe+scrape) |
| `/api/v1/media/191/play` | GET | 200 | 通过 | 播放端点可达 |
| `/api/v1/favorites` | GET | 200 | 通过 | 返回收藏列表 |
| `/api/v1/user/history` | GET | 200 | 通过 | 返回播放历史和进度 |
| `/api/v1/user/logout` | POST | 200 | 通过 | 返回 `{"ok":true}` |
| `/api/v1/admin/overview` | GET | 200 | 通过 | 返回活动日志 |
| `/api/v1/admin/users` | GET | 200 | 通过 | 返回 2 用户 (admin+viewer) |
| `/api/v1/admin/api-clients` | GET | 200 | 通过 | 返回 API 客户端列表 |
| `/api/v1/admin/access-log` | GET | 200 | 通过 | 返回访问日志 (分页) |
| `/api/v1/admin/drm-license-audit` | GET | 200 | 通过 | 返回 DRM 许可证审计记录 |
| `/api/v1/transcode/task` | GET | 200 | 通过 | 返回转码任务列表 (有 done/failed) |
| `/api/v1/preview/task` | GET | 200 | 通过 | 返回 null (无预览任务，符合预期) |
| `/api/v1/scan/task` | GET | 200 | 通过 | 返回 4 次扫描历史 |
| `/api/v1/subtitle/task` | GET | 200 | 通过 | 返回字幕任务列表 |
| `/api/v1/scrape/task` | GET | 200 | 通过 | 返回刮削任务列表 (多为 waiting) |
| `/api/v1/scrape/config` | GET | 200 | 通过 | 多提供源已配置 |
| `/api/v1/schedule/task` | GET | 200 | 通过 | 1 个定时任务 (db_optimize) |

### 发现的问题 (API 层面)

1. **大量转码任务失败** (`/api/v1/transcode/task`): 多数 HLS ABR 任务 status=failed，原因多为 `context canceled` 或 ffmpeg 崩溃。部分 `cmaf_drm` 任务因 `ffmpeg.exe: The system cannot find the path specified` 失败 (可能是路径问题)。
2. **刮削任务大量 pending**: 160+ 刮削任务处于 `waiting` 状态，刮削调度可能需要检查。
3. **字幕任务批量 pending**: 80+ 字幕任务 pending，需确认字幕处理是否正常运行。
