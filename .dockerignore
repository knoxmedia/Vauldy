# Build artifacts & runtime data
data
bin
.worktrees
*.exe

# Frontend
web/node_modules
web/dist
internal/webembed/dist

# Submodules (not part of the server image)
Vauldy-Desktop
Vauldy-ReactNative
Vauldy-TV

# Optional runtime tools (Python venvs, LibreOffice, ffmpeg binaries) —
# the container ships ffmpeg from Debian and disables venv-dependent features.
tools

# Local-only / scratch
.idea
.vscode
*.log
