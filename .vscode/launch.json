{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug knox-media server",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/cmd/server",
      "cwd": "${workspaceFolder}",
      "env": {
        "KNOX_MEDIA_CONFIG": "${workspaceFolder}/config.yml"
      }
    }
  ]
}
